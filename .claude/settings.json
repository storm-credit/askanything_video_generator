{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import sys,json,py_compile,re; m=re.search(r'\\\"file_path\\\"\\s*:\\s*\\\"([^\\\"]+\\\\.py)', sys.argv[1]); py_compile.compile(m.group(1), doraise=True) if m else None\" \"$TOOL_INPUT\" 2>&1 || echo 'Python syntax error detected'"
          }
        ]
      }
    ]
  }
}
